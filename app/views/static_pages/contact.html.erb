<div class="row">
  <div class="span6 offset4">
      <%= form_tag do %>
      <%= label_tag :name %>
      <%= text_field_tag :name, @name, :size=>50%>
      <%= label_tag :email %>
      <%= email_field_tag :email, @email, :placeholder=>"Enter your email address" %>
      <%= label_tag :message %>
      <%= text_area_tag 'message', nil, :rows => 10, :cols => 25 %> <br>


      <%# we can do it in three different ways


      <%#= select_tag 'type', options_for_select(@types.map{| value | [value, value]}, @selected_type) %> 
      <%= select_tag 'type', options_for_select(@types, @selected_type) %>  </br>


      <%#= label_tag :yes_no, "Yes"%>
      <%#= radio_button_tag :yes, :no %> 

      <%= submit_tag "Submit", class: "btn btn-large btn-primary" %>
      <%= link_to  "Send Email", email_contact_path %>
      <%end%>
    </div>
    <div class = "span3">
       <h4> Parmas: </h4>
    	<p> These params come back to view "contact" page form controller "contact". </p> </br>
    	<%= "Name :" + @name.to_s %></br>
    	<%#= "Email :" + @email.to_s %></br>
    	<%#= "Message :" + @message.to_s %></br>
    	<%#= "Type :" + @selected_type.to_s%> 

    </div>
  </div>
